---
id: <kebab-slug>
tier: L3
roles: [research | system | janitor | all]
status: draft
summary: <one line — used by ROUTING and by agents deciding whether to open you>
links: []
review_by: <YYYY-MM-DD>
---

# <Title>

> Copy this to the right `knowledge/` subfolder, fill the frontmatter, flip `status` to `active`, then **wire it in**: add a ROUTING §B row and add this `id` to the `links` of anything that references it (`governance`). An unrouted doc is an orphan at birth.

## What this is
One paragraph: the single concern this doc owns. If you need a second concern, that's a second doc (single responsibility).

## The content
Keep it terse and scannable — tables and lists over prose. Link facts that live elsewhere; never copy them (Article VI). If this passes ~300 lines, shard it (`conventions` §5).
